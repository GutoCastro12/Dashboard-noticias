# Conflitos entre relatórios de chat e o estado real do repositório

Fonte de precedência usada para resolver cada linha: HEAD do repo (`main` @ `75bd706`,
após `git fetch` + fast-forward a partir do `42e5820` originalmente informado — ver
nota no topo do `PROJECT_BIBLE.md`) > git log > arquivos versionados > GitHub Actions
(`gh run list/view`) > fatos confirmados pelo usuário em 2026-07-31 > relatórios de chat
> inferência.

Nenhum conflito foi apagado — apenas resolvido documentalmente com evidência.

| ID | Tema | Versão A (relatório) | Versão B (outro relatório / estado) | Evidência atual | Decisão canônica | Confiança | Ação necessária |
|---|---|---|---|---|---|---|---|
| C-01 | Nome do repositório | Report-1/2: "não confirmado" | Report-3: "path de log menciona Dashboard-noticias, não confirmado" | `gh repo view` → `GutoCastro12/Dashboard-noticias` [ACTIONS]; `git remote -v` [GIT] | **Dashboard-noticias**, confirmado | Alta | Nenhuma |
| C-02 | Reparo de links v2 (batchexecute) | Report-3: "entregue mas nunca confirmado em execução real com rede" | Fato do usuário 2026-07-31: já executado com rede real | Run `30607645995` (`gh run view --log`), 2026-07-31T05:44Z, job "Auditoria e reparo de links": `links_auditados=1045, fontes_principais=572, fontes_corroboradoras=473, redirects_resolvidos=453, nao_resolvidos=31, http_404_410=0, http_403_paywall=0, registros_com_campo_alterado=368` [ACTIONS][USUARIO-2026-07-31]. `risk_history.json` atual tem `links_repaired_at=2026-07-31T05:45:56Z` [REPO]. `link_debt_audit.py` contém `gnews_batchexecute`/`resolve_gnews_token` consolidados [REPO]. `test_links.py` 92/92 [REPO] | **Aplicada e confirmada com rede real.** Report-3 está desatualizado nesse ponto | Alta | Nenhuma — já resolvido |
| C-03 | Nº de links pendentes | Report-3: 373 (pós-v1) e depois "~356 medido offline sem rede" | Fato do usuário: 31 pendentes | Mesmo run acima [ACTIONS] | **31 pendentes** é o número atual e correto | Alta | Monitorar (não é falha) |
| C-04 | Score removido pela auditoria semântica | Relatório diagnóstico inicial: −2005 pontos (soma de peso-base) | Entrega de produção final do Report-3: −269 pontos (via `build_evolution`) | Não recalculável sem rodar `--reclassify-semantic-only` sobre o histórico atual; `semantic_audit.py` confirma que o score real vem de `build_evolution`, nunca de soma de peso-base [REPO] | **−269 (ou o que `build_evolution` produzir hoje) é a única metodologia válida**; qualquer número futuro de "score removido" deve vir desse cálculo | Alta (quanto à regra), não recalculada nesta tarefa (quanto ao valor atual) | Recalcular sob demanda, não nesta tarefa (somente leitura) |
| C-05 | Emissores elegíveis ao EDGAR | Report-1: 19→32 (por relação com a SEC, não país) | Report-3: 32 (config) vs. 30 (relatório de cobertura internacional, exclui Nubank/StoneCo por serem BR com ADR) | Contagem direta no `config_risco.yaml` atual: **32** emissores com `cik`/`official.sec` [REPO] | **32** é o número correto pela definição `edgar_eligible()` usada no código; 30 é um subconjunto com definição diferente (exclui BR com ADR) — ambos corretos para suas respectivas definições | Alta | Nenhuma — apenas documentar as duas definições |
| C-06 | Contagens da suíte de atribuição | Report-2: 62→68→71→75 casos ao longo do chat | Report-3: 75/75 repetido em várias entregas | `python risk_dashboard.py --test-attribution` rodado agora: **75/75** [REPO][testado nesta tarefa] | **75/75**, confirmado ao vivo | Alta | Nenhuma |
| C-07 | Arquivos que relatórios dizem existir mas não estão no repo | Report-3 cita `edgar_canonical.py`, `edgar_shadow_4h3c.py`, `test_edgar_4h3c.py`, `config_risco_4h2_candidato.yaml`, `config_risco_4h3a_candidato.yaml`, `config_risco_4h3c_candidato.yaml` como entregues/gerados | — | `ls`/`Glob` confirmam que **nenhum** desses arquivos existe no repositório atual [REPO] | Tratar como **NUNCA_APLICADO_NO_REPO** — o parser canônico do EDGAR (4H.3C) nunca chegou a ser mesclado; só existe `edgar_audit_4h2.py` (diagnóstico) e `edgar_shadow_4h3a.py`/`edgar_shadow_4h3b.py` | Alta | Nenhuma ação de código nesta tarefa; registrado como backlog |
| C-08 | `config_risco_4h3b_candidato.yaml` | Report-3: config candidato, "não subir sem aprovação" | — | Arquivo existe no repo hoje, 82KB [REPO] | Presente mas **não é o config de produção** (`config_risco.yaml` é o vigente); permanece como candidato não promovido | Alta | Decidir se deve ser removido ou mantido como referência (backlog P2) |
| C-09 | Arquivos presentes no repo não citados nos relatórios | — | — | `LICENSE`, `auditoria_cobertura_cvm.csv`, `probe_fontes_oficiais.csv` no root [REPO]; `fixtures_semantica/config_teste.yaml` e `fixtures_4h3b/config_teste.yaml` (não um `config_teste.yaml` na raiz, como relatórios sugerem) [REPO] | Documentar como achados de inventário; `config_teste.yaml` só existe dentro das pastas de fixtures, nunca na raiz | Alta | Nenhuma |
| C-10 | Estado do coletor EDGAR (coleta, não só scoring) | Report-3: implica que a coleta (`edgar_collection_enabled`) está ligada em modo sombra, só o *scoring* está desligado | — | `config_risco.yaml` atual **não define** `international_official_sources_enabled` nem `official_sources.EUA.enabled` (ambos ausentes ⇒ `False` pela função `edgar_collection_enabled`) [REPO]. Log do run mais recente do "Update Risk Dashboard" (`30629264808`, 2026-07-31T12:04Z): `"🇺🇸 SEC/EDGAR: desativado (exige international_official_sources_enabled=true E official_sources.EUA.enabled=true) — 0 filings, score inalterado."` [ACTIONS] | **A coleta do EDGAR está totalmente desativada em produção hoje**, não apenas o scoring. O "shadow mode" descrito no Report-3 só roda via `workflow_edgar_shadow.yml` isolado (`force=True`), nunca no pipeline normal | Alta | Documentado no `PROJECT_BIBLE.md` §11; nenhuma mudança de código |
| C-11 | `edgar_scoring_enabled` | Report-3: "existe no código mas nunca era consultada" (bug já corrigido) | — | `edgar_scoring_enabled()` em `risk_dashboard.py` está corretamente implementada como AND estrito com `edgar_collection_enabled()` [REPO]; ausente do `config_risco.yaml` (⇒ `False` por padrão) [REPO] | **Desligado, confirmado no código e no config** | Alta | Nenhuma — apenas manter |
| C-12 | Estado do caso Santander (Esfera/TSB/lucro) | Report-3: casos "Santander/Esfera" e "Santander/TSB" corrigidos (integração pós-aquisição/reorganização interna → não pontuam) | Fato do usuário 2026-07-31: essas correções continuam colocando o evento em `context_events_by_company`/"Contexto relacionado · não pontua", que deveria ser exclusivo para eventos de **terceiros**, não para reclassificações informativas da própria empresa monitorada | Leitura direta de `semantic_audit.py`: `EVENTO_INFORMATIVO` (linha ~663) inclui `reorganizacao_societaria_interna`, `integracao_pos_aquisicao` etc.; `apply_semantics_to_record` (linha ~742) escreve **qualquer** decisão não-pontuável (terceiro OU reclassificação informativa da própria empresa) no mesmo dicionário `context_events_by_company`; `template_risco.html.j2` renderiza `row.context_events` sob o rótulo único "Contexto relacionado · não pontua" sem diferenciar `relation_type` [REPO] | **Pendência confirmada e ainda não corrigida.** O código não distingue hoje "contexto de terceiro real" (ex.: Vale/Samarco) de "evento direto informativo/positivo da própria empresa" (ex.: Santander/Esfera, Santander/lucro pós-aquisição). Ambos caem no mesmo bucket e no mesmo rótulo de UI | Alta (confirmado por leitura de código, não apenas por relato) | **P0 no backlog** — ver `docs/BACKLOG.md` |
| C-13 | Relatórios de auditoria "desatualizados" | Report-2: `auditoria_atribuicao_entidade.csv` desatualizado; Report-3: vários CSVs de auditoria citados como "não subir ao repositório" | — | Nenhum desses CSVs de auditoria (`auditoria_atribuicao_entidade.csv`, `eventos_indiretos_reclassificados.csv`, `conflitos_direcao_evento.csv`, `comparacao_casos_rumo_vale_engie.csv` etc.) existe no repositório atual [REPO] | Tratar como artefatos de sessão de chat, nunca versionados — não são fonte de dados atual | Alta | Nenhuma |
| C-14 | Números históricos da watchlist (73→160, elegibilidade EDGAR 19/25/30/32) | Vários números ao longo dos três relatórios, ver seção 18 de cada um | — | Contagem direta do `config_risco.yaml` atual: **160 emissores** (`listada`=105, `nao_listada`=31, `fii`=8, `gestora_fundo`=16); `scoring_mode`: `normal`=144, `taxonomia_propria`=16; países: Brasil=98, EUA=25, Chile=15, México=8, outros [REPO] | Usar exclusivamente os números recalculados acima; números de "73" ou "153/177" são históricos pré-expansão, não usar como estado atual | Alta | Nenhuma |
| C-15 | Workflow `workflow_link_audit.yml`: versão read-only vs. repair | Relatórios descrevem um workflow de reparo real (v1 e v2) | Ao inspecionar o repositório antes de um `git fetch`, uma versão desatualizada e somente-leitura (`contents: read`, sem `--repair-links-only`) parecia ser a vigente | Após `git fetch origin main` (o clone local estava atrás do remoto), a versão atual em `main`@`75bd706` tem `name: "Auditoria e reparo de links"`, `permissions: contents: write`, input `mode` (`audit`/`repair`), `--repair-links-only`, exatamente batendo com o run real `30607645995` [GIT][ACTIONS] | O clone local estava desatualizado; **o workflow de reparo real está de fato versionado em `main`**, este relatório documenta a versão correta pós-fetch | Alta | Nenhuma — já resolvido; ver nota de proveniência no topo do `PROJECT_BIBLE.md` |
| C-16 | Regra de M&A ampla demais | Report-3 DEC-07: 142 rejeitados → corrigido para 29 | — | `ma_is_legitimate()` em `semantic_audit.py` implementa o default invertido (nomeia motivo de rejeição explícito) [REPO]; `test_semantica.py` 102/102 passa, incluindo casos de M&A legítimo (Viterra, Cencosud/St. Marche etc.) [testado nesta tarefa] | Regra corrigida confirmada no código e nos testes | Alta | Nenhuma |
| C-17 | `formularios_gatilho` como string vs. lista | Report-3 DEC-01: corrigido via `_normalize_edgar_forms` | — | `config_risco.yaml` **ainda armazena** `formularios_gatilho` como string literal `"['8-K', '6-K', '10-K', '10-Q', '20-F']"` (não uma lista YAML de verdade) [REPO]; porém `_normalize_edgar_forms()` em `risk_dashboard.py` converte corretamente esse formato para um `set` real de formulários [REPO] | O **valor no YAML continua no formato antigo** (string), mas o **código já tolera esse formato** — não é mais um bug funcional, é uma inconsistência estética/de higiene de config | Alta | P2 no backlog: normalizar o YAML para lista nativa, sem necessidade funcional urgente |
